# Assets Directory

Place your background texture image here as `background-texture.jpg` (or `.png`).

The image will be used as the background texture for the Proposal Builder.

Supported formats: JPG, PNG, WebP
